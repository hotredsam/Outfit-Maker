// export const LOGIN_SUCCESS = 'LOGIN_SUCCESS';

// export const loginSuccess = user => ({
//   type: LOGIN_SUCCESS,
//   payload: user,
// });

// export const login = loginData => dispatch => {
//   // You can send a POST request to your server here to authenticate the user

//   // After successful login, dispatch the action
//   const user = { id: 1, username: loginData.username }; // Replace with actual user data
//   dispatch(loginSuccess(user));
// };
